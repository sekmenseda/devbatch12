 denemesamet abd = new  denemesamet();

 //integer seda = abd.hesap(100, 50);

 system.debug(abd.hesap(100, 50));

  system.debug(abd.carp(5, 5));

  system.debug('bu bolmedir. ' + abd.bolme(5, 5));

denemesamet s1 = new denemesamet();
s1.firstName ='etka';
s1.lastName ='temur';
s1.age = 25;
s1.spor= true;
system.debug(s1);

s1.message();